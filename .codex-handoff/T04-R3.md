# Task Handoff

- Task: T04-R3 - Naver 技术预览与内部履约记录分离
- Status: completed
- Tests: `verify_all.py` passed; `verify_warehouse_shipping_batch.py` passed; `git diff --check` passed.

## 已完成

1. Naver 技术预览只返回哈希、脱敏内容、字段存在性和安全状态，不返回完整订单号或收件信息。
2. `complete_field_preview` 只返回字段存在性，不再返回完整字段值。
3. 真实订单同步使用仅在服务端内部流转的履约记录，保存发货必需的完整商品订单号和收件信息。
4. 对外同步结果只返回订单哈希，不返回内部完整订单号或收件信息。
5. 本地订单继续使用订单哈希定位，完整商品订单号保存在独立受控字段，兼容物流、刷新和时间线流程。
6. 只读刷新可以使用安全摘要；实际写入必须具有完整内部订单身份。
7. 旧隐私测试已迁移到批准合同，并保留对密钥、原始响应、日志泄露和跨用途数据暴露的阻止检查。

## 后续边界

- 生产登录会话中间件仍是独立上线前任务。
- Luna 前端接入必须使用授权 `/orders/operations` 和仓库发货接口，不得调用技术预览取得收件信息。
