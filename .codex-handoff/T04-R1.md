# Task Handoff

- Task: T04-R1 - approved warehouse-shipping privacy and approval rework
- Status: completed for backend handoff
- Base commit: `ea2440ab0168e16bca7838a2022f6d63ec750a44`
- Final commit: pending
- Files changed: secure operator access service; warehouse approval grants; order summary/operations contracts; warehouse endpoint authorization; permissions; migration support; dedicated verifier.
- Tests: `verify_warehouse_shipping_batch.py` passed on a temporary database; `npm run build` passed; `git diff --check` passed.
- Findings: old warehouse endpoints trusted body `actor_context`; R1 replaces authorization for warehouse routes with server-side user, membership and permission checks. The ordinary order route now returns a summary without PII/raw data, while a scoped operations route returns required recipient fields.
- Risks: current project has no production session middleware. `X-ERP-User-Key` is deliberately an opaque identity handoff point for that middleware; it is never used to accept client role/store claims. Frontend integration remains Luna-owned and was not changed.
- Recommended next action: integrate a real authenticated session middleware to populate the opaque user identity header, then have Luna connect the approved page flows to the new operations and warehouse approval routes.

## Delivered Rules

- Permission checks are based on active `ErpUser`, `ErpStoreMembership`, `ErpRolePermission` and `ErpPermission` rows.
- Added permission definitions for `recipient_pii.view`, `recipient_pii.export`, `recipient_pii.cs_reveal`, `shipping.batch.manage`, `shipping.writeback.approve`, and `recipient_pii.audit.read`.
- Warehouse manifest download requires `recipient_pii.export`, explicit confirmation, a user/batch/version-bound one-time token, and 10-minute expiry.
- Warehouse writeback requires `shipping.writeback.approve` and a one-time token; tracking import and confirmation increment batch version, invalidating stale tokens.
- Recipient fields appear only in the authorized operations contract and generated warehouse XLSX. Batch rows, summaries and generic order responses remove full recipient fields and `raw_data`.
- Existing batch workflow still blocks duplicate active orders and requires final confirmation before any real Naver call.
