# Task Handoff

- Task: T04-R2 - warehouse workflow recovery, audit, and privacy-test migration
- Status: partial; implementation complete, project-wide legacy privacy suite remains blocked.
- Base commit: `b56b1a3b6d3afd65aa73d4f9434e8bf576c90c15`
- Final commit: pending
- Files changed: warehouse row removal, audit events, remove endpoint, and first approved privacy assertions in `verify_all.py`.
- Tests: warehouse verifier passes; frontend build passed earlier; full `verify_all.py` reaches the legacy Naver-preview privacy suite and fails on its remaining old redaction assumptions.
- Findings: the legacy suite treats every Naver preview as a public/log response. T03.5 instead requires distinct authorized fulfillment and summary/log contracts; converting this safely needs a dedicated preview contract, not scattered assertion removal.
- Risks: do not merge this as a claim that the complete test suite passes. The R2 recovery/audit code is ready for review, but the approved full-test migration is incomplete.
- Recommended next action: total commander should assign the Naver preview contract split as a focused test/service task, then rerun `verify_all.py`.

## Implemented

- Added `POST /shipping/warehouse-batches/{batch_id}/rows/{row_id}/remove` for manual removal of an abnormal non-completed batch row. It releases the active-order lock and increments the batch version so the order can enter a newly corrected batch.
- Added no-PII audit events for recipient view, manifest export, tracking import, batch confirmation, row removal, and platform writeback result. Audit payloads contain only batch/store/action/count/reason metadata.
- Started migration of full-test expectations for approved fulfillment contact fields.
