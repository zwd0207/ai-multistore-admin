# Core-ERP-Operator-Ready-1 运营试用交付

## 交付口径

本阶段目标是让普通运营可以受控试用多店铺电商管理后台。系统优先展示本地 ERP 已保存记录、官方只读 API 能力和本地辅助功能。平台写入保持关闭。

## 每天使用顺序

1. 打开首页工作台，先看“全店铺运营总览”。
2. 先处理待发货、异常订单、库存预警。
3. 某个指标显示“?”时，表示系统无法确认该店铺的平台真实数据，不是 0。
4. 看到“IP 白名单未通过”时，先处理平台开放平台或卖家中心白名单，不要把订单数当成 0。
5. 看到“API 资料未配置完整”时，到“店铺管理”补齐对应店铺的 API 资料。
6. 进入发货辅助后，只做人工核对、导出表格、导入物流单号匹配；系统不会自动回填平台。

## 明确不会自动做的事

- 不会自动回填 Naver / Coupang 发货。
- 不会自动修改平台库存。
- 不会自动修改平台价格。
- 不会自动上下架商品。
- 不会自动回复客户。
- 不会自动提交申诉。
- 不会自动发送邮件。

## 交付前检查

在系统设置中打开“交付检查”：

- 后端服务：应显示通过。
- 当前数据源：运营试用应使用 backend，也就是读取本地保存记录。
- 平台写入关闭：必须显示已关闭。
- 店铺状态：允许存在“?”，但运营必须知道“?”表示无法确认，不是 0。
- IP 白名单：如有阻断，需要单独处理白名单或权限。
- 备份状态：交接前建议先运行备份脚本。

## 启动和备份

一键启动或重启：

```powershell
.\scripts\operator-start.ps1
```

创建本地 DB 备份：

```powershell
.\scripts\operator-db-backup.ps1
```

命令行交付检查：

```powershell
node .\scripts\operator-readiness-check.mjs
```

## 安全边界

`REAL_API_WRITE_ENABLED` 必须保持 `false`。如果交付检查显示平台写入不是关闭状态，不允许交给运营使用。

手动同步只允许读取平台并写入本地 ERP。任何真实平台写入、发货回填、改价、改库存、自动客服和自动申诉，都必须另开审批阶段。
